{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "MCP HTTP Server (port 3000)",
      "runtimeExecutable": "cargo",
      "runtimeArgs": ["run", "--release", "--", "mcp", "--http", "3000"],
      "port": 3000
    }
  ]
}
